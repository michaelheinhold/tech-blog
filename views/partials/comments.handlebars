<div class="comments">
  {{#each this}}
  <section class="single-comment">
    <div class="meta">
      {{user.username}} on {{created_at}}
    </div>
    <div class="text">
      {{text}}
    </div>
  </section>
  {{/each}}
</div>